import React, { useState } from "react";
import { Logout } from "../components/logout/logout";

const App = () => {
  return (
		<div>
			<h1>Start new blend</h1>
			<Logout />
		</div>
  );
};

export default App;
